services:
  bot:
    build: .
    container_name: bot_raskhody
    restart: unless-stopped
    env_file: .env
    environment:
      GOOGLE_CREDENTIALS_FILE: /app/credentials.json
      LIMITS_FILE: /data/limits.json
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./data:/data
